<UserControl
    x:Class="WordWiz.WordSearchResultList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WordWiz"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,Auto,*,Auto" ColumnDefinitions="*,Auto" Padding="10" RowSpacing="10">
        <TextBox x:Name="WordInputTextBox" Header="Word:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="ExplanationInputTextBox" Header="Explanation:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" AcceptsReturn="True" Height="80"/>

        <Button x:Name="AddItemButton" Content="Add Word" Click="AddItemButton_Click" Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" VerticalAlignment="Center"/>

        <ListView x:Name="ResultsListView"
                  Grid.Row="2" Grid.ColumnSpan="2"
                  ItemsSource="{x:Bind Items, Mode=OneWay}"
                  SelectionMode="Extended"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:WordItem">
                    <local:WordSearchResultItem WordDisplay="{x:Bind Word, Mode=OneWay}"
                                                ExplanationDisplay="{x:Bind Explanation, Mode=OneWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" Margin="0,10,0,0" Spacing="10">
            <Button Content="Remove Selected" Click="RemoveSelectedItemsButton_Click"/>
            <Button Content="Clear List" Click="ClearListButton_Click"/>
            <TextBlock Text="Selection Mode:" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <ComboBox x:Name="SelectionModeComboBox" SelectionChanged="SelectionModeComboBox_SelectionChanged" Width="150">
                <ComboBoxItem Content="Single" Tag="Single" IsSelected="True"/>
                <ComboBoxItem Content="Multiple" Tag="Multiple"/>
                <ComboBoxItem Content="Extended" Tag="Extended"/>
                <ComboBoxItem Content="None" Tag="None"/>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>