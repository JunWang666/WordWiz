<UserControl
    x:Class="WordWiz.WordSearchResultItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WordWiz"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="12" Spacing="4" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Margin="0,0,0,8">
        <TextBlock x:Name="WordTextBlock" Text="{x:Bind WordDisplay, Mode=OneWay}" FontWeight="Bold" FontSize="16"/>
        <TextBlock x:Name="ExplanationTextBlock" Text="{x:Bind ExplanationDisplay, Mode=OneWay}" TextWrapping="WrapWholeWords"/>
    </StackPanel>
</UserControl>