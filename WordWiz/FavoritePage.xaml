<?xml version="1.0" encoding="utf-8"?>

<Page
	x:Class="WordWiz.FavoritePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:WordWiz"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<StackPanel Padding="20" Spacing="12">
		<TextBlock Text="收藏夹" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold" />
		<StackPanel Orientation="Horizontal" Spacing="8">
			<TextBox x:Name="SearchTextBox" Width="280" PlaceholderText="筛选收藏词汇..." TextChanged="SearchTextBox_TextChanged" />
			<ComboBox x:Name="SortComboBox" Width="180" SelectedIndex="0" SelectionChanged="SortComboBox_SelectionChanged">
				<ComboBoxItem Content="最近收藏优先" />
				<ComboBoxItem Content="最早收藏优先" />
			</ComboBox>
			<Button Content="刷新" Click="RefreshButton_Click" />
		</StackPanel>
		<TextBlock x:Name="StatusTextBlock" Text="加载中..." Style="{ThemeResource CaptionTextBlockStyle}" />
		<ListView x:Name="FavoriteListView" SelectionMode="None">
			<ListView.ItemTemplate>
				<DataTemplate>
					<Grid Margin="0,4">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>
						<TextBlock Text="{Binding}" VerticalAlignment="Center" />
						<Button Grid.Column="1"
						        Content="移除"
						        Tag="{Binding}"
						        Click="RemoveFavoriteButton_Click" />
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</StackPanel>
</Page>
