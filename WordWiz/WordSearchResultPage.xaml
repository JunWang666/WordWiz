<?xml version="1.0" encoding="utf-8"?>

<Page
	x:Class="WordWiz.WordSearchResultPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:WordWiz"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid HorizontalAlignment="Stretch">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
			<ColumnDefinition Width="3*" />
		</Grid.ColumnDefinitions>
		<local:WordSearchResultList x:Name="WordSearchResultListControl"
		                            Grid.Column="0"
		                            HorizontalAlignment="Stretch"
		                            VerticalAlignment="Stretch"
		                            CurrentDetailItem="{x:Bind PageLevelSelectedItem, Mode=TwoWay}" />
		<local:WordDetails x:Name="WordDetailsControl"
		                   Grid.Column="1"
		                   HorizontalAlignment="Stretch"
		                   VerticalAlignment="Stretch"
		                   ItemToDisplay="{x:Bind PageLevelSelectedItem, Mode=OneWay}" />
	</Grid>
</Page>